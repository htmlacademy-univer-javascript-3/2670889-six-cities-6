name: Create Previews on PR

on:
  pull_request:
    branches: [master]

jobs:
  chromatic:
    name: Publish to Chromatic
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Publish to Chromatic
        id: chromatic
        uses: chromaui/action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          exitOnceUploaded: true

  codesandbox-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify project structure
        run: |
          if [ -f "package.json" ]; then
            echo "✅ package.json found"
          else
            echo "❌ package.json not found"
            exit 1
          fi

      - name: Create archive and upload to CodeSandbox
        id: upload-sandbox
        run: |
          zip -r codesandbox-upload.zip . \
            -x "*.git*" "node_modules/*" ".github/*" "*.log" "dist/*" "build/*" "coverage/*" "*.env*" "*.md"
          
          RESPONSE=$(curl -s -X POST \
            "https://codesandbox.io/api/v1/sandboxes/define" \
            -H "Content-Type: application/json" \
            -H "Accept: application/json" \
            --data @codesandbox-upload.zip \
            --max-time 30)
          
          SANDBOX_ID=$(echo "$RESPONSE" | grep -o '"sandbox_id":"[^"]*' | cut -d'"' -f4)
          
          if [ -n "$SANDBOX_ID" ]; then
            CS_URL="https://codesandbox.io/s/$SANDBOX_ID"
            echo "CODESANDBOX_URL=$CS_URL" >> $GITHUB_OUTPUT
          else
            CS_URL="https://codesandbox.io/p/github/${{ github.repository }}/${{ github.head_ref }}"
            echo "CODESANDBOX_URL=$CS_URL" >> $GITHUB_OUTPUT
          fi

  comment-previews:
    runs-on: ubuntu-latest
    needs: [chromatic, codesandbox-preview]
    if: always()
    steps:
      - name: Check job statuses
        id: check-status
        run: |
          # Проверяем статусы всех зависимых jobs
          if [[ "${{ needs.chromatic.result }}" == "success" && "${{ needs.codesandbox-preview.result }}" == "success" ]]; then
            echo "ALL_JOBS_SUCCESS=true" >> $GITHUB_OUTPUT
          else
            echo "ALL_JOBS_SUCCESS=false" >> $GITHUB_OUTPUT
            echo "Some jobs failed:"
            echo "Chromatic: ${{ needs.chromatic.result }}"
            echo "CodeSandbox: ${{ needs.codesandbox-preview.result }}"
          fi

      - name: Comment on PR
        uses: actions/github-script@v7
        if: steps.check-status.outputs.ALL_JOBS_SUCCESS == 'true'
        with:
          script: |
            const csUrl = '${{ steps.upload-sandbox.outputs.CODESANDBOX_URL }}';
            const chromaticBuildUrl = '${{ steps.chromatic.outputs.STORYBOOK_URL }}';
            const ghPagesUrl = 'https://aasemikov.github.io/2670889-six-cities-6/';
            
            // Статичная ссылка на ваш Chromatic проект
            const chromaticProjectUrl = 'https://www.chromatic.com/build?appId=691b0d625e34626933fd4919';
            
            let message = '🌐 **Live Previews:**\n\n';
            
            // Chromatic Storybook - используем статичную ссылку на проект
            message += `🎨 [Storybook on Chromatic](${chromaticProjectUrl}) - компоненты и UI-тесты\n`;
            
            // Если есть конкретная сборка, добавляем и её
            if (chromaticBuildUrl && chromaticBuildUrl.includes('chromatic.com')) {
              message += `   ↳ [Текущая сборка](${chromaticBuildUrl})\n`;
            }
            
            // CodeSandbox
            if (csUrl.includes('/s/')) {
              message += `📦 [CodeSandbox](${csUrl}) - готовый sandbox\n`;
            } else {
              message += `📦 [CodeSandbox](${csUrl}) - импорт и запуск\n`;
            }
            
            // GitHub Pages
            message += `🚀 [GitHub Pages](${ghPagesUrl}) - деплой приложения\n\n`;
            
            message += '---\n';
            message += '_Previews обновляются автоматически при каждом пуше_';
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });